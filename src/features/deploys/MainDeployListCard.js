import React from 'react';
import MainDeployList from './MainDeployList'

export default class MainDeployListCard extends React.Component {
  render() {
    return(
      <MainDeployList/>
    )
  }
}